import LoginPage from '../../components/LoginPage';

export default function Register() {
  // Pass mode="register" so it shows the registration form by default
  return <LoginPage mode="register" />;
}
